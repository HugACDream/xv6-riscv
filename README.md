# xv6 笔记
## lab1

实验一主要是调用系统调用进行实现，需要注意的是main函数需要检查参数是否合法（通过argc和argv传参）

### sleep

直接调用sleep即可

### pingpong

利用管道进行子进程与父进程的通信
功能要求是先发送ping再发送pong。wait函数可以使父进程等待子进程执行完，所以可以子进程打印ping，父进程打印pong。（后来查看代码，由于read的阻塞，实际上并不需要wait。。。）
复习一下fork：原进程从fork处创建出一个子进程，子进程出了fork的返回值与父进程完全一样（copy的），区分二者的方法是令int pid = fork()，然后用条件分支判断pid值即可。
先建立两个管道，分别用于父子进程的读写，记作p1，p2。
父进程：关p1写，关p2读，向p2写，关闭p2写，读取p1，关p1读，打印信息，等待子进程结束
子进程：关p2写，关p1读，读取p2，打印信息，写p1，关p1写（实际read和write的顺序就注定了子进程打印再父进程之前）

### 素数
并发的素数筛法有点像埃氏筛
使用的实现方法是递归，
递归传的参数是管道。递归的终止条件就是read == 0（管道读不到了）
父进程获取当前的管道中的最小值，最小值没有被之前的素数整除过所以一定是素数
子进程遍历当前管道中的数字，如果不能被这个素数整除，就说明还有机会成为素数，继续通过管道传下去，越传越少，最后筛出结果。

### find
实现方法也是递归，递归参数是path和目标文件name。
用到了一个新的调用：fstat（看英文猜大概是文件状态的意思吧），本次主要用到了类型（st.type）。类型是文件就比对文件名，相同就输出path；类型是目录就继续递归（.和..以及大小为0的目录不递归）。
每次递归时的path都是相对于当前目录（./）的所以每次递归需要将子目录拼接到父目录的后面。

### xargs
最开始没看懂xargs的功能，阅读了一下几位大佬的博客后大致理解了
大致功能从标准输入（stdin）读取数据，并将其作为参数传递给另一个命令。
实现思路是先将xargs的参数从args中分出来（舍弃了第一个调用名（例如echo），并给后边xargs参数预留一个空间）
然后先读取命令参数，再分行处理（利用strchr查找\n）
子进程用于调用exce实现功能，父进程用于删除已执行完的行。